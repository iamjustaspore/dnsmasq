name: Build dnsmasq
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build dnsmasq
      run: |
        sudo apt-get update
        sudo apt-get install build-essential
        ./configure
        make
